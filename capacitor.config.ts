import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'basmile.tech.mondentiste',
  appName: 'mondentiste',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
